import { ClickpropDirective } from './clickprop.directive';

describe('ClickpropDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickpropDirective();
    expect(directive).toBeTruthy();
  });
});
